# Path to store tutor data (progress, sessions)
# In development, this is ignored - the app uses ./.dev-data instead
DATA_PATH=

# OAuth token from Claude Max subscription
# Generate with: claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=
